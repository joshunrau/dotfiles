{{- $localBinDir := "$HOME/.bin" -}}

{{- if eq .chezmoi.hostname "dnpws15" -}}
{{- $localBinDir = "/local/bin" -}}
{{- end -}}

[data]
    localBinDir = "{{ $localBinDir }}"
[git]
    autoCommit = true
    autoPush = true
