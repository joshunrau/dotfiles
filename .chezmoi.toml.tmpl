{{- $localDir:= "$HOME/.local" -}}

{{- if eq .chezmoi.hostname "dnpws15" -}}
{{- $localDir = "/local" -}}
{{- end -}}

[data]
    localDir = "{{ $localDir }}"
    pyenvRoot = "{{ $localDir }}/opt/pyenv"
[git]
    autoCommit = true
    autoPush = true
